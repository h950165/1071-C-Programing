#include<stdio>

void sorting(int d[], int n)
{
    int temp;
    for(int i=0;i,n-1;i++)
    for(int j=0)    
    {
     if(d[j]>d[j+1])
      {
       temp = d{j};
       d[j]=d[j+1];
       d[j+1] = teap;   
      }
    }
}
